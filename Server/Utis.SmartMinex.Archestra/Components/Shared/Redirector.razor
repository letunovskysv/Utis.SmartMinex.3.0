@*--------------------------------------------------------------------------------------------------
// (С) 2017-2025 ООО УралТехИС. Интеллектуальная Системная Платформа 3.0. Все права защищены.
// Описание: Redirector – Необходим для повторного создания экземпляра формы при открытие её с
// различными параметрами, в противном случае не будет создаваться новый экземпляр страницы.
//------------------------------------------------------------------------------------------------*@
@page "/_/{url1?}/{url2?}/{url3?}"
@inject NavigationManager _nav

@code {
    public const string Prefix = "_/";

    [Parameter]
    public string? Url1 { get; set; }
    [Parameter]
    public string? Url2 { get; set; }
    [Parameter]
    public string? Url3 { get; set; }

    protected override void OnInitialized() =>
        _nav.NavigateTo(string.Join("/", new[] { Url1, Url2, Url3 }));
}
